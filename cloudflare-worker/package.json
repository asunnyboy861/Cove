{
  "name": "cove-token-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "init": "wrangler login"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
